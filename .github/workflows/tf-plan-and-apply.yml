name: tf-plan-and-apply

on:
  pull_request:
    branches: [ main ]
  push:
    tags:
      - 'v*-test'
      - 'v*-staging'
      - 'v*-prod'

permissions:
  id-token: write
  contents: read

concurrency:
  group: terraform-${{ github.ref }}
  cancel-in-progress: false

env:
  AWS_REGION: eu-west-2
  TF_IN_AUTOMATION: "true"
  TF_INPUT: "false"

jobs:
  detect-environment:
    runs-on: ubuntu-latest
    outputs:
      environment:   ${{ steps.parse.outputs.environment }}     # test|staging|production
      role_arn:      ${{ steps.parse.outputs.role_arn }}
      is_deploy:     ${{ steps.parse.outputs.is_deploy }}       # true on tag pushes
      layers_json:   ${{ steps.detect.outputs.layers_json }}
      state_bucket:  ${{ steps.derive.outputs.state_bucket }}
      lock_table:    ${{ steps.derive.outputs.lock_table }}
    steps:
      - name: Parse Environment from Tag (or PR→test)
        id: parse
        shell: bash
        run: |
          ref="${GITHUB_REF}"
          ENV_SLUG="test"
          IS_DEPLOY="false"
          ROLE="arn:aws:iam::137167813802:role/kuflink-test-github-oidc-terraform"

          if [[ "$ref" =~ refs/tags/v.*-(test|staging|prod)$ ]]; then
            case "${BASH_REMATCH[1]}" in
              test)    ENV_SLUG="test";       ROLE="arn:aws:iam::137167813802:role/kuflink-test-github-oidc-terraform" ;;
              staging) ENV_SLUG="staging";    ROLE="arn:aws:iam::222222222222:role/kuflink-staging-github-oidc-terraform" ;;
              prod)    ENV_SLUG="production"; ROLE="arn:aws:iam::333333333333:role/kuflink-prod-github-oidc-terraform" ;;
            esac
            IS_DEPLOY="true"
          fi

          echo "environment=$ENV_SLUG" >> $GITHUB_OUTPUT
          echo "is_deploy=$IS_DEPLOY"  >> $GITHUB_OUTPUT
          echo "role_arn=$ROLE"        >> $GITHUB_OUTPUT

      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      - name: Ensure jq is available
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Detect Changed Layers (env-nested, cascade, ordered)
        id: detect
        shell: bash
        run: |
          set -euo pipefail
          ENV_SLUG="${{ steps.parse.outputs.environment }}"

          # Build change list
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch --no-tags origin "${{ github.base_ref }}" --depth=1
            CHANGED="$(git diff --name-only "origin/${{ github.base_ref }}"...HEAD || true)"
          else
            if git rev-parse -q --verify HEAD^ >/dev/null; then
              CHANGED="$(git diff --name-only HEAD^ HEAD || true)"
            else
              CHANGED="$(git ls-tree -r --name-only HEAD || true)"
            fi
          fi
          echo "Changed files:"; printf "%s\n" "$CHANGED"

          # Detect direct layer touches
          need_foundation=false
          need_platform=false
          need_data=false
          need_apps=false

          while IFS= read -r f; do
            [[ -z "$f" ]] && continue

            # env/layer dirs
            [[ "$f" =~ ^environments/${ENV_SLUG}/foundation/ ]] && need_foundation=true
            [[ "$f" =~ ^environments/${ENV_SLUG}/platform/   ]] && need_platform=true
            [[ "$f" =~ ^environments/${ENV_SLUG}/data/       ]] && need_data=true
            [[ "$f" =~ ^environments/${ENV_SLUG}/apps/       ]] && need_apps=true

            # modules → owning layers
            [[ "$f" =~ ^modules/vpc/|^modules/iam/|^modules/parameter-store/ ]] && need_foundation=true
            [[ "$f" =~ ^modules/eb-waf/|^modules/s3-admin-waf/               ]] && need_platform=true
            [[ "$f" =~ ^modules/rds/|^modules/rds-restored/|^modules/redshift/|^modules/redshift-restored/|^modules/dms-mysql-to-redshift/|^modules/elastic-cache-redis/ ]] && need_data=true
            [[ "$f" =~ ^modules/ec2-|^modules/s3-admin/|^modules/s3-frontend/|^modules/eb-api/ ]] && need_apps=true
          done <<< "$CHANGED"

          # ----- Cascade rules -----
          # foundation ⇒ platform,data,apps
          if $need_foundation; then
            need_platform=true
            need_data=true
            need_apps=true
          fi
          # platform ⇒ data,apps
          if $need_platform; then
            need_data=true
            need_apps=true
          fi
          # data ⇒ apps
          if $need_data; then
            need_apps=true
          fi

          # Build ordered unique list
          LAYERS=()
          $need_foundation && LAYERS+=("foundation")
          $need_platform   && LAYERS+=("platform")
          $need_data       && LAYERS+=("data")
          $need_apps       && LAYERS+=("apps")

          # On tag deploy, if *nothing* matched, deploy everything (promotion)
          if [[ ${#LAYERS[@]} -eq 0 && "${{ steps.parse.outputs.is_deploy }}" == "true" ]]; then
            LAYERS=("foundation" "platform" "data" "apps")
          fi

          # Emit compact JSON for downstream matrix/contains()
          JSON=$(printf '%s\n' "${LAYERS[@]}" | jq -R . | jq -sc .)
          echo "layers_json=$JSON" >> "$GITHUB_OUTPUT"
          echo "Detected layers after cascade (ordered): $JSON"


      - name: Derive backend bucket & lock table per environment
        id: derive
        shell: bash
        run: |
          case "${{ steps.parse.outputs.environment }}" in
            test)
              STATE_BUCKET="kuflink-test-states"
              LOCK_TABLE="kuflink-tf-locks-test"
              ;;
            staging)
              STATE_BUCKET="kuflink-staging-states"
              LOCK_TABLE="kuflink-tf-locks-staging"
              ;;
            production)
              STATE_BUCKET="kuflink-prod-states"
              LOCK_TABLE="kuflink-tf-locks-prod"
              ;;
          esac
          echo "state_bucket=$STATE_BUCKET" >> $GITHUB_OUTPUT
          echo "lock_table=$LOCK_TABLE"     >> $GITHUB_OUTPUT
          echo "Using backend: bucket=$STATE_BUCKET, lock=$LOCK_TABLE"

  # Helper reusable step (YAML anchor-like via 'run'): init with CLI backend config
  # We'll just repeat the same run block in each job for clarity.

  # =======================
  # Layer jobs (sequential)
  # =======================

  terraform-foundation:
    needs: detect-environment
    if: contains(fromJson(needs.detect-environment.outputs.layers_json), 'foundation')
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT:  ${{ needs.detect-environment.outputs.environment }}
      ROLE_ARN:     ${{ needs.detect-environment.outputs.role_arn }}
      IS_DEPLOY:    ${{ needs.detect-environment.outputs.is_deploy }}
      LAYER:        foundation
      AWS_REGION:  ${{ vars.AWS_REGION || 'eu-west-2' }}
      TF_STATE_BUCKET: ${{ needs.detect-environment.outputs.state_bucket }}
      TF_LOCK_TABLE:   ${{ needs.detect-environment.outputs.lock_table }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      # (Optional) Bootstrap backend infra if missing — uncomment if you want first-run creation
      # - name: Ensure backend bucket & lock table exist
      #   shell: bash
      #   run: |
      #     set -e
      #     if ! aws s3api head-bucket --bucket "${TF_STATE_BUCKET}" 2>/dev/null; then
      #       echo "Creating S3 bucket ${TF_STATE_BUCKET}"
      #       aws s3api create-bucket --bucket "${TF_STATE_BUCKET}" --region "${AWS_REGION}" --create-bucket-configuration LocationConstraint="${AWS_REGION}"
      #     fi
      #     if ! aws dynamodb describe-table --table-name "${TF_LOCK_TABLE}" >/dev/null 2>&1; then
      #       echo "Creating DynamoDB table ${TF_LOCK_TABLE}"
      #       aws dynamodb create-table \
      #         --table-name "${TF_LOCK_TABLE}" \
      #         --attribute-definitions AttributeName=LockID,AttributeType=S \
      #         --key-schema AttributeName=LockID,KeyType=HASH \
      #         --billing-mode PAY_PER_REQUEST
      #       aws dynamodb wait table-exists --table-name "${TF_LOCK_TABLE}"
      #     fi

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }

      - name: Terraform Init (CLI backend)
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${ENVIRONMENT}/${LAYER}/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="encrypt=true"

      - name: Terraform Fmt & Validate
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          terraform fmt -check
          terraform validate

      - name: Terraform Plan
        id: plan
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        continue-on-error: true
        run: |
          set +e
          terraform plan -no-color -out=tfplan.bin -detailed-exitcode -var="environment=${ENVIRONMENT}"
          code=$?
          echo "exit_code=$code" >> $GITHUB_OUTPUT
          if [ "$code" -eq 2 ]; then echo "has_changes=true" >> $GITHUB_OUTPUT; exit 0
          elif [ "$code" -eq 0 ]; then echo "has_changes=false" >> $GITHUB_OUTPUT; exit 0
          else exit "$code"; fi

      - name: Upload Plan
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ env.ENVIRONMENT }}-${{ env.LAYER }}
          path: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}/tfplan.bin
          if-no-files-found: ignore

      - name: Terraform Apply
        if: env.IS_DEPLOY == 'true' && steps.plan.outputs.has_changes == 'true'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: terraform apply -auto-approve tfplan.bin

  terraform-platform:
    needs: terraform-foundation
    if: contains(fromJson(needs.detect-environment.outputs.layers_json), 'platform')
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT:  ${{ needs.detect-environment.outputs.environment }}
      ROLE_ARN:     ${{ needs.detect-environment.outputs.role_arn }}
      IS_DEPLOY:    ${{ needs.detect-environment.outputs.is_deploy }}
      LAYER:        platform
      AWS_REGION:   ${{ vars.AWS_REGION }}
      TF_STATE_BUCKET: ${{ needs.detect-environment.outputs.state_bucket }}
      TF_LOCK_TABLE:   ${{ needs.detect-environment.outputs.lock_table }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }
      - name: Terraform Init (CLI backend)
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${ENVIRONMENT}/${LAYER}/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="encrypt=true"
      - name: Terraform Fmt & Validate
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          terraform fmt -check
          terraform validate
      - name: Terraform Plan
        id: plan
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        continue-on-error: true
        run: |
          set +e
          terraform plan -no-color -out=tfplan.bin -detailed-exitcode -var="environment=${ENVIRONMENT}"
          code=$?
          echo "exit_code=$code" >> $GITHUB_OUTPUT
          if [ "$code" -eq 2 ]; then echo "has_changes=true" >> $GITHUB_OUTPUT; exit 0
          elif [ "$code" -eq 0 ]; then echo "has_changes=false" >> $GITHUB_OUTPUT; exit 0
          else exit "$code"; fi
      - name: Upload Plan
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ env.ENVIRONMENT }}-${{ env.LAYER }}
          path: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}/tfplan.bin
          if-no-files-found: ignore
      - name: Terraform Apply
        if: env.IS_DEPLOY == 'true' && steps.plan.outputs.has_changes == 'true'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: terraform apply -auto-approve tfplan.bin

  terraform-data:
    needs: terraform-platform
    if: contains(fromJson(needs.detect-environment.outputs.layers_json), 'data')
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT:  ${{ needs.detect-environment.outputs.environment }}
      ROLE_ARN:     ${{ needs.detect-environment.outputs.role_arn }}
      IS_DEPLOY:    ${{ needs.detect-environment.outputs.is_deploy }}
      LAYER:        data
      AWS_REGION:   ${{ vars.AWS_REGION }}
      TF_STATE_BUCKET: ${{ needs.detect-environment.outputs.state_bucket }}
      TF_LOCK_TABLE:   ${{ needs.detect-environment.outputs.lock_table }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }
      - name: Terraform Init (CLI backend)
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${ENVIRONMENT}/${LAYER}/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="encrypt=true"
      - name: Terraform Fmt & Validate
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          terraform fmt -check
          terraform validate
      - name: Terraform Plan
        id: plan
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        continue-on-error: true
        run: |
          set +e
          terraform plan -no-color -out=tfplan.bin -detailed-exitcode -var="environment=${ENVIRONMENT}"
          code=$?
          echo "exit_code=$code" >> $GITHUB_OUTPUT
          if [ "$code" -eq 2 ]; then echo "has_changes=true" >> $GITHUB_OUTPUT; exit 0
          elif [ "$code" -eq 0 ]; then echo "has_changes=false" >> $GITHUB_OUTPUT; exit 0
          else exit "$code"; fi
      - name: Upload Plan
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ env.ENVIRONMENT }}-${{ env.LAYER }}
          path: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}/tfplan.bin
          if-no-files-found: ignore
      - name: Terraform Apply
        if: env.IS_DEPLOY == 'true' && steps.plan.outputs.has_changes == 'true'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: terraform apply -auto-approve tfplan.bin

  terraform-apps:
    needs: terraform-data
    if: contains(fromJson(needs.detect-environment.outputs.layers_json), 'apps')
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT:  ${{ needs.detect-environment.outputs.environment }}
      ROLE_ARN:     ${{ needs.detect-environment.outputs.role_arn }}
      IS_DEPLOY:    ${{ needs.detect-environment.outputs.is_deploy }}
      LAYER:        apps
      AWS_REGION:   ${{ vars.AWS_REGION }}
      TF_STATE_BUCKET: ${{ needs.detect-environment.outputs.state_bucket }}
      TF_LOCK_TABLE:   ${{ needs.detect-environment.outputs.lock_table }}
    steps:
      - uses: actions/checkout@v4
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }
      - name: Terraform Init (CLI backend)
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${ENVIRONMENT}/${LAYER}/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="encrypt=true"
      - name: Terraform Fmt & Validate
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          terraform fmt -check
          terraform validate
      - name: Terraform Plan
        id: plan
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        continue-on-error: true
        run: |
          set +e
          terraform plan -no-color -out=tfplan.bin -detailed-exitcode -var="environment=${ENVIRONMENT}"
          code=$?
          echo "exit_code=$code" >> $GITHUB_OUTPUT
          if [ "$code" -eq 2 ]; then echo "has_changes=true" >> $GITHUB_OUTPUT; exit 0
          elif [ "$code" -eq 0 ]; then echo "has_changes=false" >> $GITHUB_OUTPUT; exit 0
          else exit "$code"; fi
      - name: Upload Plan
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ env.ENVIRONMENT }}-${{ env.LAYER }}
          path: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}/tfplan.bin
          if-no-files-found: ignore
      - name: Terraform Apply
        if: env.IS_DEPLOY == 'true' && steps.plan.outputs.has_changes == 'true'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: terraform apply -auto-approve tfplan.bin
