name: tf-destroy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to destroy'
        required: true
        type: choice
        options:
          - test
          - staging
          # - production  # Uncomment only if you really need this
      destroy_all_layers:
        description: 'Destroy ALL layers (apps, data, platform, foundation)'
        type: boolean
        default: false
      destroy_apps:
        description: 'Destroy APPS layer'
        type: boolean
        default: false
      destroy_data:
        description: 'Destroy DATA layer'
        type: boolean
        default: false
      destroy_platform:
        description: 'Destroy PLATFORM layer'
        type: boolean
        default: false
      destroy_foundation:
        description: 'Destroy FOUNDATION layer'
        type: boolean
        default: false
      confirm_destroy:
        description: 'Type "DESTROY" to confirm (case-sensitive)'
        required: true
      auto_approve:
        description: 'Skip destroy plan approval (NOT recommended for staging/production)'
        type: boolean
        default: false
      force_continue_on_failure:
        description: '⚠️ Continue destroying next layers even if previous fails (DANGEROUS!)'
        type: boolean
        default: false

permissions:
  id-token: write
  contents: read

concurrency:
  group: terraform-destroy-${{ github.event.inputs.environment }}
  cancel-in-progress: false

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  TF_IN_AUTOMATION: "true"
  TF_INPUT: "false"

jobs:
  validate-destroy:
    runs-on: ubuntu-latest
    outputs:
      can_proceed: ${{ steps.check.outputs.can_proceed }}
      state_bucket: ${{ steps.check.outputs.state_bucket }}
      lock_table: ${{ steps.check.outputs.lock_table }}
      layers_to_destroy: ${{ steps.check.outputs.layers_to_destroy }}
    steps:
      - name: Validate Confirmation
        id: check
        run: |
          # Validate confirmation text
          if [[ "${{ github.event.inputs.confirm_destroy }}" != "DESTROY" ]]; then
            echo "❌ Confirmation text must be exactly 'DESTROY'"
            exit 1
          fi
          
          # Set environment-specific values
          ENV="${{ github.event.inputs.environment }}"
          case "$ENV" in
            test)
              STATE_BUCKET="kuflink-test-states"
              LOCK_TABLE="kuflink-tf-locks-test"
              ;;
            staging)
              STATE_BUCKET="kuflink-staging-states"
              LOCK_TABLE="kuflink-tf-locks-staging"
              ;;
            production)
              STATE_BUCKET="kuflink-prod-states"
              LOCK_TABLE="kuflink-tf-locks-prod"
              ;;
            *)
              echo "❌ Unsupported environment: $ENV"
              exit 1
              ;;
          esac
          
          echo "state_bucket=$STATE_BUCKET" >> "$GITHUB_OUTPUT"
          echo "lock_table=$LOCK_TABLE" >> "$GITHUB_OUTPUT"
          
          # Determine layers to destroy from checkboxes
          DESTROY_ALL="${{ github.event.inputs.destroy_all_layers }}"
          DESTROY_APPS="${{ github.event.inputs.destroy_apps }}"
          DESTROY_DATA="${{ github.event.inputs.destroy_data }}"
          DESTROY_PLATFORM="${{ github.event.inputs.destroy_platform }}"
          DESTROY_FOUNDATION="${{ github.event.inputs.destroy_foundation }}"
          
          LAYERS=()
          
          if [[ "$DESTROY_ALL" == "true" ]]; then
            # Full destroy in safe order
            LAYERS=("apps" "data" "platform" "foundation")
          else
            # Always respect safe destroy order:
            # apps -> data -> platform -> foundation
            if [[ "$DESTROY_APPS" == "true" ]]; then
              LAYERS+=("apps")
            fi
            if [[ "$DESTROY_DATA" == "true" ]]; then
              LAYERS+=("data")
            fi
            if [[ "$DESTROY_PLATFORM" == "true" ]]; then
              LAYERS+=("platform")
            fi
            if [[ "$DESTROY_FOUNDATION" == "true" ]]; then
              LAYERS+=("foundation")
            fi
          fi
          
          if [[ ${#LAYERS[@]} -eq 0 ]]; then
            echo "❌ No layers selected to destroy."
            echo "   Tick 'Destroy ALL layers' or one/more individual layer checkboxes."
            exit 1
          fi
          
          LAYERS_JSON=$(printf '%s\n' "${LAYERS[@]}" | jq -R . | jq -sc .)
          
          echo "layers_to_destroy=$LAYERS_JSON" >> "$GITHUB_OUTPUT"
          echo "can_proceed=true" >> "$GITHUB_OUTPUT"
          
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎯 Environment: $ENV"
          echo "🗑️  Layers to destroy (in order): $LAYERS_JSON"
          if [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
            echo "⚠️  FORCE CONTINUE ENABLED - will continue even if layers fail!"
          fi
          echo "⚠️  THIS ACTION IS IRREVERSIBLE!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  # Destroy jobs in REVERSE dependency order: apps -> data -> platform -> foundation
  
  destroy-apps:
    needs: validate-destroy
    if: contains(fromJson(needs.validate-destroy.outputs.layers_to_destroy), 'apps')
    runs-on: ubuntu-latest
    environment: 
      name: ${{ github.event.inputs.environment }}-destroy
    env:
      ENVIRONMENT: ${{ github.event.inputs.environment }}
      LAYER: apps
      AWS_REGION: ${{ vars.AWS_REGION }}
      TF_STATE_BUCKET: ${{ needs.validate-destroy.outputs.state_bucket }}
      TF_LOCK_TABLE: ${{ needs.validate-destroy.outputs.lock_table }}
    outputs:
      status: ${{ steps.final_status.outputs.status }}
    steps:
      - uses: actions/checkout@v4
      
      - name: Determine AWS Role
        id: aws_role
        run: |
          case "${{ env.ENVIRONMENT }}" in
            test)
              echo "role_arn=${{ secrets.TEST_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
            staging)
              echo "role_arn=${{ secrets.STAGING_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
            production)
              echo "role_arn=${{ secrets.PRODUCTION_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
          esac
      
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.aws_role.outputs.role_arn }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }
      
      - name: Terraform Init
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${ENVIRONMENT}/${LAYER}/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="encrypt=true"
      
      - name: Terraform Destroy Plan
        id: plan
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          set +e
          terraform plan -destroy -no-color -out=destroy.tfplan -var="environment=${ENVIRONMENT}" 2>&1 | tee destroy_plan_output.txt
          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          
          if [ $EXIT_CODE -eq 0 ] && [ -f destroy.tfplan ]; then
            echo "plan_success=true" >> "$GITHUB_OUTPUT"
          else
            echo "plan_success=false" >> "$GITHUB_OUTPUT"
            if [ $EXIT_CODE -ne 0 ]; then
              echo "❌ terraform plan failed with exit code $EXIT_CODE"
            else
              echo "❌ terraform plan reported success but destroy.tfplan is missing"
            fi
          fi
          
          # Do not fail here; final status step will decide
          exit 0
      
      - name: Show Destroy Plan
        if: steps.plan.outputs.plan_success == 'true'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        run: |
          if [ ! -f destroy.tfplan ]; then
            echo "::warning::destroy.tfplan not found, skipping terraform show"
            exit 0
          fi
          terraform show destroy.tfplan
      
      - name: Terraform Destroy (Attempt 1)
        id: destroy_attempt1
        if: |
          steps.plan.outputs.plan_success == 'true' &&
          (github.event.inputs.auto_approve == 'true' || github.event.inputs.environment == 'test')
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          terraform apply -auto-approve destroy.tfplan 2>&1 | tee destroy_attempt1_output.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      
      - name: Wait Before Retry
        if: steps.destroy_attempt1.outcome == 'failure'
        run: |
          echo "⚠️ First destroy attempt failed. Waiting 30 seconds before retry..."
          sleep 30
      
      - name: Refresh State and Retry Destroy
        id: destroy_attempt2
        if: steps.destroy_attempt1.outcome == 'failure'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          echo "🔄 Refreshing state and retrying destroy..."
          terraform refresh -var="environment=${ENVIRONMENT}" || true
          terraform destroy -auto-approve -var="environment=${ENVIRONMENT}" 2>&1 | tee destroy_attempt2_output.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      
      - name: Final Status Check
        id: final_status
        if: always()
        run: |
          if [[ "${{ steps.plan.outputs.plan_success }}" != "true" ]]; then
            echo "❌ Destroy plan failed (no valid plan available)"
            echo "status=failed" >> "$GITHUB_OUTPUT"
            if [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
              echo "⚠️ Force-continue enabled, allowing workflow to proceed"
              echo "status=failed_forced" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            exit 1
          elif [[ "${{ steps.destroy_attempt2.outcome }}" == "success" ]]; then
            echo "✅ Apps layer destroyed successfully on retry"
            echo "status=success" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ steps.destroy_attempt1.outcome }}" == "success" ]]; then
            echo "✅ Apps layer destroyed successfully"
            echo "status=success" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
            echo "⚠️ Apps layer destroy FAILED but force-continue is enabled"
            echo "status=failed_forced" >> "$GITHUB_OUTPUT"
            exit 0
          else
            echo "❌ Apps layer destroy failed after retries"
            echo "status=failed" >> "$GITHUB_OUTPUT"
            echo "::error::Apps layer destroy failed. Review logs and manually clean up if needed."
            exit 1
          fi
      
      - name: Generate Summary
        if: always()
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          echo "## Terraform Destroy Summary - ${{ env.LAYER }} (${{ env.ENVIRONMENT }})" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          
          if [ -f destroy_plan_output.txt ]; then
            PLAN_SUMMARY=$(grep -E "Plan:|No changes\." destroy_plan_output.txt | tail -1 || echo "Destroy plan completed")
            echo "### 📋 Destroy Plan" >> "$GITHUB_STEP_SUMMARY"
            echo "$PLAN_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy plan output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_plan_output.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          
          if [ -f destroy_attempt1_output.txt ]; then
            sed 's/\x1b\[[0-9;]*m//g' destroy_attempt1_output.txt > destroy_attempt1_clean.txt
            DESTROY_SUMMARY=$(grep -E "Destroy complete!|Apply complete!" destroy_attempt1_clean.txt | tail -1 || echo "Destroy attempt 1 completed")
            
            if [[ "${{ steps.destroy_attempt1.outcome }}" == "success" ]]; then
              echo "### ✅ Destroy (Attempt 1 - Success)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "### ❌ Destroy (Attempt 1 - Failed)" >> "$GITHUB_STEP_SUMMARY"
            fi
            echo "$DESTROY_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy attempt 1 output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_attempt1_clean.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          
          if [ -f destroy_attempt2_output.txt ]; then
            sed 's/\x1b\[[0-9;]*m//g' destroy_attempt2_output.txt > destroy_attempt2_clean.txt
            DESTROY_SUMMARY=$(grep -E "Destroy complete!|Apply complete!" destroy_attempt2_clean.txt | tail -1 || echo "Destroy attempt 2 completed")
            
            if [[ "${{ steps.destroy_attempt2.outcome }}" == "success" ]]; then
              echo "### ✅ Destroy (Attempt 2 - Success after retry)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "### ❌ Destroy (Attempt 2 - Failed)" >> "$GITHUB_STEP_SUMMARY"
            fi
            echo "$DESTROY_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy attempt 2 output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_attempt2_clean.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi

  destroy-data:
    needs: [validate-destroy, destroy-apps]
    if: |
      ((!failure() && !cancelled()) || github.event.inputs.force_continue_on_failure == 'true') &&
      contains(fromJson(needs.validate-destroy.outputs.layers_to_destroy), 'data')
    runs-on: ubuntu-latest
    environment: 
      name: ${{ github.event.inputs.environment }}-destroy
    env:
      ENVIRONMENT: ${{ github.event.inputs.environment }}
      LAYER: data
      AWS_REGION: ${{ vars.AWS_REGION }}
      TF_STATE_BUCKET: ${{ needs.validate-destroy.outputs.state_bucket }}
      TF_LOCK_TABLE: ${{ needs.validate-destroy.outputs.lock_table }}
    outputs:
      status: ${{ steps.final_status.outputs.status }}
    steps:
      - name: Check Previous Layer Status
        if: needs.destroy-apps.outputs.status == 'failed_forced'
        run: |
          echo "::warning::⚠️ Previous layer (apps) failed but continuing due to force flag"
      
      - uses: actions/checkout@v4
      
      - name: Determine AWS Role
        id: aws_role
        run: |
          case "${{ env.ENVIRONMENT }}" in
            test)
              echo "role_arn=${{ secrets.TEST_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
            staging)
              echo "role_arn=${{ secrets.STAGING_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
            production)
              echo "role_arn=${{ secrets.PRODUCTION_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
          esac
      
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.aws_role.outputs.role_arn }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }
      
      - name: Terraform Init
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${ENVIRONMENT}/${LAYER}/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="encrypt=true"
      
      - name: Terraform Destroy Plan
        id: plan
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          set +e
          terraform plan -destroy -no-color -out=destroy.tfplan -var="environment=${ENVIRONMENT}" 2>&1 | tee destroy_plan_output.txt
          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          
          if [ $EXIT_CODE -eq 0 ] && [ -f destroy.tfplan ]; then
            echo "plan_success=true" >> "$GITHUB_OUTPUT"
          else
            echo "plan_success=false" >> "$GITHUB_OUTPUT"
            if [ $EXIT_CODE -ne 0 ]; then
              echo "❌ terraform plan failed with exit code $EXIT_CODE"
            else
              echo "❌ terraform plan reported success but destroy.tfplan is missing"
            fi
          fi
          
          exit 0
      
      - name: Show Destroy Plan
        if: steps.plan.outputs.plan_success == 'true'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        run: |
          if [ ! -f destroy.tfplan ]; then
            echo "::warning::destroy.tfplan not found, skipping terraform show"
            exit 0
          fi
          terraform show destroy.tfplan
      
      - name: Terraform Destroy (Attempt 1)
        id: destroy_attempt1
        if: |
          steps.plan.outputs.plan_success == 'true' &&
          (github.event.inputs.auto_approve == 'true' || github.event.inputs.environment == 'test')
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          terraform apply -auto-approve destroy.tfplan 2>&1 | tee destroy_attempt1_output.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      
      - name: Wait Before Retry
        if: steps.destroy_attempt1.outcome == 'failure'
        run: |
          echo "⚠️ First destroy attempt failed. Waiting 30 seconds before retry..."
          sleep 30
      
      - name: Refresh State and Retry Destroy
        id: destroy_attempt2
        if: steps.destroy_attempt1.outcome == 'failure'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          echo "🔄 Refreshing state and retrying destroy..."
          terraform refresh -var="environment=${ENVIRONMENT}" || true
          terraform destroy -auto-approve -var="environment=${ENVIRONMENT}" 2>&1 | tee destroy_attempt2_output.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      
      - name: Final Status Check
        id: final_status
        if: always()
        run: |
          if [[ "${{ steps.plan.outputs.plan_success }}" != "true" ]]; then
            echo "❌ Destroy plan failed (no valid plan available)"
            echo "status=failed" >> "$GITHUB_OUTPUT"
            if [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
              echo "⚠️ Force-continue enabled, allowing workflow to proceed"
              echo "status=failed_forced" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            exit 1
          elif [[ "${{ steps.destroy_attempt2.outcome }}" == "success" ]]; then
            echo "✅ Data layer destroyed successfully on retry"
            echo "status=success" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ steps.destroy_attempt1.outcome }}" == "success" ]]; then
            echo "✅ Data layer destroyed successfully"
            echo "status=success" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
            echo "⚠️ Data layer destroy FAILED but force-continue is enabled"
            echo "status=failed_forced" >> "$GITHUB_OUTPUT"
            exit 0
          else
            echo "❌ Data layer destroy failed after retries"
            echo "status=failed" >> "$GITHUB_OUTPUT"
            echo "::error::Data layer destroy failed. Review logs and manually clean up if needed."
            exit 1
          fi
      
      - name: Generate Summary
        if: always()
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          echo "## Terraform Destroy Summary - ${{ env.LAYER }} (${{ env.ENVIRONMENT }})" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          
          if [ -f destroy_plan_output.txt ]; then
            PLAN_SUMMARY=$(grep -E "Plan:|No changes\." destroy_plan_output.txt | tail -1 || echo "Destroy plan completed")
            echo "### 📋 Destroy Plan" >> "$GITHUB_STEP_SUMMARY"
            echo "$PLAN_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy plan output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_plan_output.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          
          if [ -f destroy_attempt1_output.txt ]; then
            sed 's/\x1b\[[0-9;]*m//g' destroy_attempt1_output.txt > destroy_attempt1_clean.txt
            DESTROY_SUMMARY=$(grep -E "Destroy complete!|Apply complete!" destroy_attempt1_clean.txt | tail -1 || echo "Destroy attempt 1 completed")
            
            if [[ "${{ steps.destroy_attempt1.outcome }}" == "success" ]]; then
              echo "### ✅ Destroy (Attempt 1 - Success)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "### ❌ Destroy (Attempt 1 - Failed)" >> "$GITHUB_STEP_SUMMARY"
            fi
            echo "$DESTROY_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy attempt 1 output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_attempt1_clean.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          
          if [ -f destroy_attempt2_output.txt ]; then
            sed 's/\x1b\[[0-9;]*m//g' destroy_attempt2_output.txt > destroy_attempt2_clean.txt
            DESTROY_SUMMARY=$(grep -E "Destroy complete!|Apply complete!" destroy_attempt2_clean.txt | tail -1 || echo "Destroy attempt 2 completed")
            
            if [[ "${{ steps.destroy_attempt2.outcome }}" == "success" ]]; then
              echo "### ✅ Destroy (Attempt 2 - Success after retry)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "### ❌ Destroy (Attempt 2 - Failed)" >> "$GITHUB_STEP_SUMMARY"
            fi
            echo "$DESTROY_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy attempt 2 output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_attempt2_clean.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi

  destroy-platform:
    needs: [validate-destroy, destroy-data]
    if: |
      ((!failure() && !cancelled()) || github.event.inputs.force_continue_on_failure == 'true') &&
      contains(fromJson(needs.validate-destroy.outputs.layers_to_destroy), 'platform')
    runs-on: ubuntu-latest
    environment: 
      name: ${{ github.event.inputs.environment }}-destroy
    env:
      ENVIRONMENT: ${{ github.event.inputs.environment }}
      LAYER: platform
      AWS_REGION: ${{ vars.AWS_REGION }}
      TF_STATE_BUCKET: ${{ needs.validate-destroy.outputs.state_bucket }}
      TF_LOCK_TABLE: ${{ needs.validate-destroy.outputs.lock_table }}
    outputs:
      status: ${{ steps.final_status.outputs.status }}
    steps:
      - name: Check Previous Layer Status
        if: needs.destroy-data.outputs.status == 'failed_forced'
        run: |
          echo "::warning::⚠️ Previous layer (data) failed but continuing due to force flag"
      
      - uses: actions/checkout@v4
      
      - name: Determine AWS Role
        id: aws_role
        run: |
          case "${{ env.ENVIRONMENT }}" in
            test)
              echo "role_arn=${{ secrets.TEST_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
            staging)
              echo "role_arn=${{ secrets.STAGING_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
            production)
              echo "role_arn=${{ secrets.PRODUCTION_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
          esac
      
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.aws_role.outputs.role_arn }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }
      
      - name: Terraform Init
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${ENVIRONMENT}/${LAYER}/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="encrypt=true"
      
      - name: Terraform Destroy Plan
        id: plan
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          set +e
          terraform plan -destroy -no-color -out=destroy.tfplan -var="environment=${ENVIRONMENT}" 2>&1 | tee destroy_plan_output.txt
          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          
          if [ $EXIT_CODE -eq 0 ] && [ -f destroy.tfplan ]; then
            echo "plan_success=true" >> "$GITHUB_OUTPUT"
          else
            echo "plan_success=false" >> "$GITHUB_OUTPUT"
            if [ $EXIT_CODE -ne 0 ]; then
              echo "❌ terraform plan failed with exit code $EXIT_CODE"
            else
              echo "❌ terraform plan reported success but destroy.tfplan is missing"
            fi
          fi
          
          exit 0
      
      - name: Show Destroy Plan
        if: steps.plan.outputs.plan_success == 'true'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        run: |
          if [ ! -f destroy.tfplan ]; then
            echo "::warning::destroy.tfplan not found, skipping terraform show"
            exit 0
          fi
          terraform show destroy.tfplan
      
      - name: Terraform Destroy (Attempt 1)
        id: destroy_attempt1
        if: |
          steps.plan.outputs.plan_success == 'true' &&
          (github.event.inputs.auto_approve == 'true' || github.event.inputs.environment == 'test')
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          terraform apply -auto-approve destroy.tfplan 2>&1 | tee destroy_attempt1_output.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      
      - name: Wait Before Retry
        if: steps.destroy_attempt1.outcome == 'failure'
        run: |
          echo "⚠️ First destroy attempt failed. Waiting 30 seconds before retry..."
          sleep 30
      
      - name: Refresh State and Retry Destroy
        id: destroy_attempt2
        if: steps.destroy_attempt1.outcome == 'failure'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          echo "🔄 Refreshing state and retrying destroy..."
          terraform refresh -var="environment=${ENVIRONMENT}" || true
          terraform destroy -auto-approve -var="environment=${ENVIRONMENT}" 2>&1 | tee destroy_attempt2_output.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      
      - name: Final Status Check
        id: final_status
        if: always()
        run: |
          if [[ "${{ steps.plan.outputs.plan_success }}" != "true" ]]; then
            echo "❌ Destroy plan failed (no valid plan available)"
            echo "status=failed" >> "$GITHUB_OUTPUT"
            if [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
              echo "⚠️ Force-continue enabled, allowing workflow to proceed"
              echo "status=failed_forced" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            exit 1
          elif [[ "${{ steps.destroy_attempt2.outcome }}" == "success" ]]; then
            echo "✅ Platform layer destroyed successfully on retry"
            echo "status=success" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ steps.destroy_attempt1.outcome }}" == "success" ]]; then
            echo "✅ Platform layer destroyed successfully"
            echo "status=success" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
            echo "⚠️ Platform layer destroy FAILED but force-continue is enabled"
            echo "status=failed_forced" >> "$GITHUB_OUTPUT"
            exit 0
          else
            echo "❌ Platform layer destroy failed after retries"
            echo "status=failed" >> "$GITHUB_OUTPUT"
            echo "::error::Platform layer destroy failed. Review logs and manually clean up if needed."
            exit 1
          fi
      
      - name: Generate Summary
        if: always()
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          echo "## Terraform Destroy Summary - ${{ env.LAYER }} (${{ env.ENVIRONMENT }})" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          
          if [ -f destroy_plan_output.txt ]; then
            PLAN_SUMMARY=$(grep -E "Plan:|No changes\." destroy_plan_output.txt | tail -1 || echo "Destroy plan completed")
            echo "### 📋 Destroy Plan" >> "$GITHUB_STEP_SUMMARY"
            echo "$PLAN_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy plan output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_plan_output.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          
          if [ -f destroy_attempt1_output.txt ]; then
            sed 's/\x1b\[[0-9;]*m//g' destroy_attempt1_output.txt > destroy_attempt1_clean.txt
            DESTROY_SUMMARY=$(grep -E "Destroy complete!|Apply complete!" destroy_attempt1_clean.txt | tail -1 || echo "Destroy attempt 1 completed")
            
            if [[ "${{ steps.destroy_attempt1.outcome }}" == "success" ]]; then
              echo "### ✅ Destroy (Attempt 1 - Success)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "### ❌ Destroy (Attempt 1 - Failed)" >> "$GITHUB_STEP_SUMMARY"
            fi
            echo "$DESTROY_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy attempt 1 output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_attempt1_clean.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          
          if [ -f destroy_attempt2_output.txt ]; then
            sed 's/\x1b\[[0-9;]*m//g' destroy_attempt2_output.txt > destroy_attempt2_clean.txt
            DESTROY_SUMMARY=$(grep -E "Destroy complete!|Apply complete!" destroy_attempt2_clean.txt | tail -1 || echo "Destroy attempt 2 completed")
            
            if [[ "${{ steps.destroy_attempt2.outcome }}" == "success" ]]; then
              echo "### ✅ Destroy (Attempt 2 - Success after retry)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "### ❌ Destroy (Attempt 2 - Failed)" >> "$GITHUB_STEP_SUMMARY"
            fi
            echo "$DESTROY_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy attempt 2 output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_attempt2_clean.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi

  destroy-foundation:
    needs: [validate-destroy, destroy-platform]
    if: |
      ((!failure() && !cancelled()) || github.event.inputs.force_continue_on_failure == 'true') &&
      contains(fromJson(needs.validate-destroy.outputs.layers_to_destroy), 'foundation')
    runs-on: ubuntu-latest
    environment: 
      name: ${{ github.event.inputs.environment }}-destroy
    env:
      ENVIRONMENT: ${{ github.event.inputs.environment }}
      LAYER: foundation
      AWS_REGION: ${{ vars.AWS_REGION }}
      TF_STATE_BUCKET: ${{ needs.validate-destroy.outputs.state_bucket }}
      TF_LOCK_TABLE: ${{ needs.validate-destroy.outputs.lock_table }}
    outputs:
      status: ${{ steps.final_status.outputs.status }}
    steps:
      - name: Check Previous Layer Status
        if: needs.destroy-platform.outputs.status == 'failed_forced'
        run: |
          echo "::warning::⚠️ Previous layer (platform) failed but continuing due to force flag"
      
      - uses: actions/checkout@v4
      
      - name: Determine AWS Role
        id: aws_role
        run: |
          case "${{ env.ENVIRONMENT }}" in
            test)
              echo "role_arn=${{ secrets.TEST_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
            staging)
              echo "role_arn=${{ secrets.STAGING_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
            production)
              echo "role_arn=${{ secrets.PRODUCTION_AWS_OIDC_ROLE_ARN }}" >> "$GITHUB_OUTPUT"
              ;;
          esac
      
      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ steps.aws_role.outputs.role_arn }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with: { terraform_wrapper: false }
      
      - name: Terraform Init
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="key=${ENVIRONMENT}/${LAYER}/terraform.tfstate" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="encrypt=true"
      
      - name: Terraform Destroy Plan
        id: plan
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          set +e
          terraform plan -destroy -no-color -out=destroy.tfplan -var="environment=${ENVIRONMENT}" 2>&1 | tee destroy_plan_output.txt
          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          
          if [ $EXIT_CODE -eq 0 ] && [ -f destroy.tfplan ]; then
            echo "plan_success=true" >> "$GITHUB_OUTPUT"
          else
            echo "plan_success=false" >> "$GITHUB_OUTPUT"
            if [ $EXIT_CODE -ne 0 ]; then
              echo "❌ terraform plan failed with exit code $EXIT_CODE"
            else
              echo "❌ terraform plan reported success but destroy.tfplan is missing"
            fi
          fi
          
          exit 0
      
      - name: Show Destroy Plan
        if: steps.plan.outputs.plan_success == 'true'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        run: |
          if [ ! -f destroy.tfplan ]; then
            echo "::warning::destroy.tfplan not found, skipping terraform show"
            exit 0
          fi
          terraform show destroy.tfplan
      
      - name: Terraform Destroy (Attempt 1)
        id: destroy_attempt1
        if: |
          steps.plan.outputs.plan_success == 'true' &&
          (github.event.inputs.auto_approve == 'true' || github.event.inputs.environment == 'test')
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          terraform apply -auto-approve destroy.tfplan 2>&1 | tee destroy_attempt1_output.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      
      - name: Wait Before Retry
        if: steps.destroy_attempt1.outcome == 'failure'
        run: |
          echo "⚠️ First destroy attempt failed. Waiting 30 seconds before retry..."
          sleep 30
      
      - name: Refresh State and Retry Destroy
        id: destroy_attempt2
        if: steps.destroy_attempt1.outcome == 'failure'
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        continue-on-error: true
        run: |
          echo "🔄 Refreshing state and retrying destroy..."
          terraform refresh -var="environment=${ENVIRONMENT}" || true
          terraform destroy -auto-approve -var="environment=${ENVIRONMENT}" 2>&1 | tee destroy_attempt2_output.txt
          echo "exit_code=$?" >> "$GITHUB_OUTPUT"
      
      - name: Final Status Check
        id: final_status
        if: always()
        run: |
          if [[ "${{ steps.plan.outputs.plan_success }}" != "true" ]]; then
            echo "❌ Destroy plan failed (no valid plan available)"
            echo "status=failed" >> "$GITHUB_OUTPUT"
            if [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
              echo "⚠️ Force-continue enabled, allowing workflow to proceed"
              echo "status=failed_forced" >> "$GITHUB_OUTPUT"
              exit 0
            fi
            exit 1
          elif [[ "${{ steps.destroy_attempt2.outcome }}" == "success" ]]; then
            echo "✅ Foundation layer destroyed successfully on retry"
            echo "status=success" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ steps.destroy_attempt1.outcome }}" == "success" ]]; then
            echo "✅ Foundation layer destroyed successfully"
            echo "status=success" >> "$GITHUB_OUTPUT"
            exit 0
          elif [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
            echo "⚠️ Foundation layer destroy FAILED but force-continue is enabled"
            echo "status=failed_forced" >> "$GITHUB_OUTPUT"
            exit 0
          else
            echo "❌ Foundation layer destroy failed after retries"
            echo "status=failed" >> "$GITHUB_OUTPUT"
            echo "::error::Foundation layer destroy failed. Review logs and manually clean up if needed."
            exit 1
          fi
      
      - name: Generate Summary
        if: always()
        working-directory: environments/${{ env.ENVIRONMENT }}/${{ env.LAYER }}
        shell: bash
        run: |
          echo "## Terraform Destroy Summary - ${{ env.LAYER }} (${{ env.ENVIRONMENT }})" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          
          if [ -f destroy_plan_output.txt ]; then
            PLAN_SUMMARY=$(grep -E "Plan:|No changes\." destroy_plan_output.txt | tail -1 || echo "Destroy plan completed")
            echo "### 📋 Destroy Plan" >> "$GITHUB_STEP_SUMMARY"
            echo "$PLAN_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy plan output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_plan_output.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          
          if [ -f destroy_attempt1_output.txt ]; then
            sed 's/\x1b\[[0-9;]*m//g' destroy_attempt1_output.txt > destroy_attempt1_clean.txt
            DESTROY_SUMMARY=$(grep -E "Destroy complete!|Apply complete!" destroy_attempt1_clean.txt | tail -1 || echo "Destroy attempt 1 completed")
            
            if [[ "${{ steps.destroy_attempt1.outcome }}" == "success" ]]; then
              echo "### ✅ Destroy (Attempt 1 - Success)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "### ❌ Destroy (Attempt 1 - Failed)" >> "$GITHUB_STEP_SUMMARY"
            fi
            echo "$DESTROY_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy attempt 1 output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_attempt1_clean.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi
          
          if [ -f destroy_attempt2_output.txt ]; then
            sed 's/\x1b\[[0-9;]*m//g' destroy_attempt2_output.txt > destroy_attempt2_clean.txt
            DESTROY_SUMMARY=$(grep -E "Destroy complete!|Apply complete!" destroy_attempt2_clean.txt | tail -1 || echo "Destroy attempt 2 completed")
            
            if [[ "${{ steps.destroy_attempt2.outcome }}" == "success" ]]; then
              echo "### ✅ Destroy (Attempt 2 - Success after retry)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "### ❌ Destroy (Attempt 2 - Failed)" >> "$GITHUB_STEP_SUMMARY"
            fi
            echo "$DESTROY_SUMMARY" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "<details><summary>View full destroy attempt 2 output</summary>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat destroy_attempt2_clean.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "</details>" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
          fi

  summary:
    needs: [validate-destroy, destroy-apps, destroy-data, destroy-platform, destroy-foundation]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Destroy Summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 DESTROY WORKFLOW SUMMARY"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Environment: ${{ github.event.inputs.environment }}"
          echo ""
          
          # Apps status
          if [[ "${{ contains(fromJson(needs.validate-destroy.outputs.layers_to_destroy), 'apps') }}" == "true" ]]; then
            if [[ "${{ needs.destroy-apps.outputs.status }}" == "success" ]]; then
              echo "✅ Apps: Destroyed successfully"
            elif [[ "${{ needs.destroy-apps.outputs.status }}" == "failed_forced" ]]; then
              echo "⚠️ Apps: FAILED but continued due to force flag"
            else
              echo "❌ Apps: Failed to destroy"
            fi
          fi
          
          # Data status
          if [[ "${{ contains(fromJson(needs.validate-destroy.outputs.layers_to_destroy), 'data') }}" == "true" ]]; then
            if [[ "${{ needs.destroy-data.outputs.status }}" == "success" ]]; then
              echo "✅ Data: Destroyed successfully"
            elif [[ "${{ needs.destroy-data.outputs.status }}" == "failed_forced" ]]; then
              echo "⚠️ Data: FAILED but continued due to force flag"
            elif [[ "${{ needs.destroy-data.result }}" == "skipped" ]]; then
              echo "⏭️ Data: Skipped (previous layer failed)"
            else
              echo "❌ Data: Failed to destroy"
            fi
          fi
          
          # Platform status
          if [[ "${{ contains(fromJson(needs.validate-destroy.outputs.layers_to_destroy), 'platform') }}" == "true" ]]; then
            if [[ "${{ needs.destroy-platform.outputs.status }}" == "success" ]]; then
              echo "✅ Platform: Destroyed successfully"
            elif [[ "${{ needs.destroy-platform.outputs.status }}" == "failed_forced" ]]; then
              echo "⚠️ Platform: FAILED but continued due to force flag"
            elif [[ "${{ needs.destroy-platform.result }}" == "skipped" ]]; then
              echo "⏭️ Platform: Skipped (previous layer failed)"
            else
              echo "❌ Platform: Failed to destroy"
            fi
          fi
          
          # Foundation status
          if [[ "${{ contains(fromJson(needs.validate-destroy.outputs.layers_to_destroy), 'foundation') }}" == "true" ]]; then
            if [[ "${{ needs.destroy-foundation.outputs.status }}" == "success" ]]; then
              echo "✅ Foundation: Destroyed successfully"
            elif [[ "${{ needs.destroy-foundation.outputs.status }}" == "failed_forced" ]]; then
              echo "⚠️ Foundation: FAILED but continued due to force flag"
            elif [[ "${{ needs.destroy-foundation.result }}" == "skipped" ]]; then
              echo "⏭️ Foundation: Skipped (previous layer failed)"
            else
              echo "❌ Foundation: Failed to destroy"
            fi
          fi
          
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          # Overall status
          if [[ "${{ github.event.inputs.force_continue_on_failure }}" == "true" ]]; then
            echo "⚠️ Force-continue was enabled. Check individual layer statuses above."
            echo "⚠️ Manual cleanup may be required for failed layers."
          fi
